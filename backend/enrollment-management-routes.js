const express=require('express');
const mongoose=require('mongoose');
const crypto=require('crypto');
require('dotenv').config();
const clean=(v,max)=>typeof v==='string'?v.trim().slice(0,max):'';
const verifyAdmin=req=>{try{const h=String(req.headers.authorization||'');if(!h.startsWith('Bearer '))return false;const t=h.slice(7),p=t.split('.'),secret=process.env.ADMIN_SESSION_SECRET||'',email=(process.env.ADMIN_EMAIL||'Abdallah-Shalaby1@outlook.com').trim().toLowerCase();if(!secret||p.length!==2)return false;const sig=crypto.createHmac('sha256',secret).update(p[0]).digest('base64url'),a=Buffer.from(p[1]),b=Buffer.from(sig);if(a.length!==b.length||!crypto.timingSafeEqual(a,b))return false;const x=JSON.parse(Buffer.from(p[0],'base64url').toString('utf8'));return !!x.exp&&x.exp>Date.now()&&String(x.email||'').toLowerCase()===email}catch{return false}};
const allowed=new Set(['pending','confirmed','active','completed','cancelled']);
const view=x=>({enrollmentId:x.enrollmentId,traineeId:x.traineeId,courseId:x.courseId,courseNameEn:x.courseNameEn,courseNameAr:x.courseNameAr,registrationType:x.registrationType,companyName:x.companyName||'',companyContact:x.companyContact||'',companyEmail:x.companyEmail||'',status:x.status,registeredAt:x.registeredAt||x.createdAt||null,updatedAt:x.updatedAt||null});
const listHandler=async(req,res)=>{if(!verifyAdmin(req))return res.status(401).json({message:'Admin authentication required'});try{const q=clean(req.query?.q,180).toLowerCase(),status=clean(req.query?.status,30),filter={};if(status)filter.status=status;let rows=await mongoose.connection.collection('enrollments').find(filter).sort({registeredAt:-1}).limit(20000).toArray();if(q)rows=rows.filter(x=>[x.enrollmentId,x.traineeId,x.courseNameEn,x.courseNameAr,x.companyName,x.registrationType].some(v=>String(v||'').toLowerCase().includes(q)));res.json(rows.map(view))}catch(e){console.error('Enrollment list error',e);res.status(500).json({message:'Unable to load enrollments'})}};
const statusHandler=async(req,res)=>{if(!verifyAdmin(req))return res.status(401).json({message:'Admin authentication required'});try{const enrollmentId=clean(req.params.enrollmentId,100),status=clean(req.body?.status,30);if(!enrollmentId||!allowed.has(status))return res.status(400).json({message:'Invalid enrollment status'});const c=mongoose.connection.collection('enrollments');const current=await c.findOne({enrollmentId});if(!current)return res.status(404).json({message:'Enrollment not found'});await c.updateOne({enrollmentId},{$set:{status,updatedAt:new Date()}});const row=await c.findOne({enrollmentId});res.json({ok:true,...view(row)})}catch(e){console.error('Enrollment status update error',e);res.status(500).json({message:'Unable to update enrollment status: '+clean(e.message,240)})}};
if(!express.application.__si_enrollment_management_methods){
 express.application.__si_enrollment_management_methods=true;
 const originalGet=express.application.get,originalPatch=express.application.patch,originalPost=express.application.post;
 express.application.get=function(route,...handlers){if(route==='/api/admin/enrollments')return originalGet.call(this,route,listHandler);return originalGet.call(this,route,...handlers)};
 express.application.patch=function(route,...handlers){if(route==='/api/admin/enrollments/:enrollmentId/status')return originalPatch.call(this,route,statusHandler);return originalPatch.call(this,route,...handlers)};
 express.application.post=function(route,...handlers){if(route==='/api/admin/enrollments/:enrollmentId/status')return originalPost.call(this,route,statusHandler);return originalPost.call(this,route,...handlers)};
}
