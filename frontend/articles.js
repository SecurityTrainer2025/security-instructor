const API=(window.APP_CONFIG&&window.APP_CONFIG.API_BASE)||'https://security-instructor.onrender.com';
const esc=s=>String(s??'').replace(/[&<>\"]/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[m]));
const para=s=>esc(s).split(/\r?\n+/).filter(Boolean).map(x=>'<p>'+x+'</p>').join('');
fetch(API+'/api/articles',{cache:'no-store'}).then(r=>r.json()).then(rows=>{const el=document.getElementById('articleList');el.innerHTML=rows.length?rows.map((a,i)=>`<article class="article" id="article-${i+1}"><div class="article-head"><img src="${esc(a.authorImage||'Abdallah_Abdelaziz_Portrait.jpg')}" alt="${esc(a.authorName)}"><div><div class="kicker">${esc(a.category)}</div><h2>${esc(a.titleEn)}</h2><h3 dir="rtl">${esc(a.titleAr)}</h3><div class="byline">By / بقلم: <b>${esc(a.authorName)}</b> · ${esc(a.authorTitle)} · ${new Date(a.createdAt).toLocaleDateString()}</div></div></div><div class="columns"><div><p><b>${esc(a.excerptEn)}</b></p>${para(a.contentEn)}</div><div class="ar-lead"><p><b>${esc(a.excerptAr)}</b></p>${para(a.contentAr)}</div></div><div class="read"><a href="article-view.html?slug=${encodeURIComponent(a.slug)}">Open Article / فتح المقال</a></div></article>`).join(''):'<div class="paper-note">No approved articles yet. / لا توجد مقالات معتمدة بعد.</div>'}).catch(()=>{document.getElementById('articleList').innerHTML='<div class="paper-note">Unable to load articles right now. / تعذر تحميل المقالات حاليًا.</div>'});