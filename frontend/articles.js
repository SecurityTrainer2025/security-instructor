const API=(window.APP_CONFIG&&window.APP_CONFIG.API_BASE)||'https://security-instructor.onrender.com';
const esc=s=>String(s??'').replace(/[&<>\"]/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[m]));
const text=s=>esc(s).replace(/\r?\n/g,'<br><br>');
fetch(API+'/api/articles',{cache:'no-store'}).then(r=>r.json()).then(rows=>{const stack=document.getElementById('articleStack');if(!rows.length){stack.innerHTML='<div class="panel" style="padding:40px;text-align:center"><p>No approved articles yet. / لا توجد مقالات معتمدة بعد.</p></div>';return}stack.innerHTML=rows.map((a,i)=>`<article class="newspaper-article" id="article-${i}"><div class="article-meta">${esc(a.category)} · ${new Date(a.createdAt).toLocaleDateString()}</div><div class="article-author"><img src="${esc(a.authorImage||'Abdallah_Abdelaziz_Portrait.jpg')}" alt="${esc(a.authorName)}"><div><strong>${esc(a.authorName)}</strong><div style="color:#b7c4ce;margin-top:5px">${esc(a.authorTitle)}</div></div></div><div class="bilingual-columns"><div class="language-column ar"><h3>العربية</h3><h2>${esc(a.titleAr)}</h2><p>${text(a.contentAr)}</p></div><div class="language-column en"><h3>ENGLISH</h3><h2>${esc(a.titleEn)}</h2><p>${text(a.contentEn)}</p></div></div><div class="read-more"><a class="btn gold" href="article-view.html?slug=${encodeURIComponent(a.slug)}">Read Full Article / قراءة المقال</a></div></article>`).join('');let idx=0;const count=document.getElementById('articleCount');const update=()=>{const els=[...stack.querySelectorAll('.newspaper-article')];const top=stack.scrollTop;let best=0,bestD=Infinity;els.forEach((el,i)=>{const d=Math.abs(el.offsetTop-top);if(d<bestD){bestD=d;best=i}});idx=best;if(count)count.textContent=(idx+1)+' / '+els.length+' articles'};const scrollTo=i=>{const els=stack.querySelectorAll('.newspaper-article');if(!els.length)return;idx=Math.max(0,Math.min(i,els.length-1));stack.scrollTo({top:els[idx].offsetTop,behavior:'smooth'});update()};document.getElementById('prev').onclick=()=>scrollTo(idx-1);document.getElementById('next').onclick=()=>scrollTo(idx+1);stack.addEventListener('scroll',()=>requestAnimationFrame(update));document.addEventListener('keydown',e=>{if(e.key==='ArrowUp'||e.key==='ArrowLeft')scrollTo(idx-1);if(e.key==='ArrowDown'||e.key==='ArrowRight')scrollTo(idx+1)});update()}).catch(()=>{document.getElementById('articleStack').innerHTML='<div class="panel" style="padding:40px;text-align:center"><p>Unable to load articles right now. / تعذر تحميل المقالات حاليًا.</p></div>'});